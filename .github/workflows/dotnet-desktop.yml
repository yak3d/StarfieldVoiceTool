name: StarfieldVT Build

on:
  push:
    branches:
      - main
    tags:
      - 'v**'
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    strategy:
      matrix:
        include:
          - os: windows-latest
            rid: win-x64
            artifact-name: starfieldvt-windows
          - os: ubuntu-latest
            rid: linux-x64
            artifact-name: starfieldvt-linux

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          submodules: true

      - name: Get short SHA (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: echo "SHORT_SHA=$('${{ github.sha }}'.Substring(0, 8))" >> $env:GITHUB_ENV

      - name: Get short SHA (Linux)
        if: runner.os == 'Linux'
        run: echo "SHORT_SHA=$(echo '${{ github.sha }}' | cut -c1-8)" >> $GITHUB_ENV

      - name: Install .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - name: Restore
        run: dotnet restore StarfieldVT.sln

      - name: Publish
        run: dotnet publish StarfieldVT/StarfieldVT.csproj -c Release -r ${{ matrix.rid }} --self-contained true -o publish/${{ matrix.rid }}

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        if: github.ref == 'refs/heads/main' || startsWith(github.ref, 'refs/tags/')
        with:
          name: ${{ matrix.artifact-name }}-${{ env.SHORT_SHA }}
          path: publish/${{ matrix.rid }}

  release:
    runs-on: ubuntu-latest
    needs: build
    if: startsWith(github.ref, 'refs/tags/')
    permissions:
      contents: write
    steps:
      - name: Set short git commit SHA
        run: echo "COMMIT_SHORT_SHA=$(echo '${{ github.sha }}' | cut -c1-8)" >> $GITHUB_ENV

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Zip artifacts
        run: |
          for dir in artifacts/*/; do
            name=$(basename "$dir")
            zip -r "${name}-${{ github.ref_name }}.zip" "$dir"
          done

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: "*.zip"
